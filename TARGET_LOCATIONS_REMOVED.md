# Target locations removed

The Target locations setting has been removed from the Content Manager and from the article-generation prompt.

Geographic relevance is now driven by the SEO keyword library, including keywords generated by website-content analysis.

Apply the migration:

`npx supabase db push`

Then redeploy:

`npx supabase functions deploy generate-content-worker`
